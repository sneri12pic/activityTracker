# Flutter's Gradle plugin injects its own keep rules; manifest-referenced
# classes (activity, services, widget receivers) are kept automatically.
# Add app-specific keep rules here if a release build hits ClassNotFoundException.
